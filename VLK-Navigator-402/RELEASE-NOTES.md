# v27.1 — Clinical Polish

- SVG 38 px / 34 px; плашки 50 px / 46 px. Збережено розміри карток і вертикальні позиції назв/лічильників у компонентному стенді.
- Психіатр: мозковий контур + діалог; дерматовенеролог: лупа + поверхня і шари шкіри; хірург: розділені руків’я, шийка й лезо скальпеля.
- Стрічка редакції виведена під header поза умовою showDashboard. Вона статична, повністю переноситься й відділяє дату корпусу від записаної дати перевірки джерела.
- DOM-контролер очищає завершені/перервані анімації; одна картка або емблема активна одночасно. Один tap не затримує дію.
- Clinical Signature — короткий одноразовий штрих 1100 мс; основні specialty-послідовності 1800–2200 мс.
- Додано 15 тестів; виправлено accessibility-інваріант стрічки, без послаблення нормативних тестів.
- 146 захищених файлів SHA-256 ідентичні v27, app/page.tsx має лише дві render-зміни.
- 127/127 source-only, 63/63 ізольованих browser-перевірки, 16/16 layout/theme. Обов’язкові lint/build/npm test: BLOCKED через npm registry, НЕ PASS.
- Повний звіт: `QA-V27.1-CLINICAL-POLISH.md`. Історичні записи нижче не є перевіркою цього випуску.

---

# v23.1 — читабельніше друковане зведення

- Довгі офіційні URL у друці замінені компактними підписами зі збереженим href.
- Дослівний текст і копіювання незмінні; додано два тести друкованого компонента.
- 110 тестів PASS; обидві збірки PASS; перевірено ресурси production-сервера.
- Перевірки та обмеження: `QA-V23-1.md`.

---

# v23 — зручніший робочий екран і мобільна навігація

- Світліша панель статей, чіткіша ієрархія тексту, спокійні поверхні та картки спеціальностей з піктограмами; темна тема збережена.
- На екранах до 1280 px — вкладки «Статті», «Стаття», «Зведення» з окремим прокручуванням і клавіатурним перемиканням.
- Вибір графи доступний одразу; категорія й додатковий фільтр згорнуті в «Параметри огляду».
- Запит «47б» відкриває потрібний пункт також при виборі з бічного списку. Вибраний пункт прокручується в область читання.
- Ctrl/⌘K фокусує пошук. Додано кнопки попередньої та наступної статті.
- Якщо браузер відмовляє в автоматичному копіюванні, доступне вікно з повним текстом для ручного копіювання.
- Друк зведення використовує окремий друкований шар поточної сторінки без нового спливного вікна.
- Нормативні дані збережено побайтово; 108 тестів PASS. Межі перевірки: `QA-V23.md`.

---

# v22 — графи та копіювання формулювання пункту

- Додано окремий вибір графи I / II / III без неявного ототожнення з категорією
  оглядуваного.
- Для обраної графи показуються тільки ті абзаци офіційного пояснення, де її
  названо прямо; за відсутності згадки інтерфейс чесно повідомляє про це.
- Дослівний результат пункту не змінюється і не видається за автоматичне рішення ВЛК.
- Обрана графа зберігається локально та входить до копійованого тексту і зведення.
- У розгорнутій картці пункту додано кнопку «Скопіювати формулювання пункту».
- Додано регресійні тести діапазонів I–III / II–III, захист від плутання зі
  стадіями хвороби та перевірки реальних статей 56 і 62.
- Нормативний корпус не редагувався. Подробиці: `QA-V22.md`.

---

# v21 — пошук, таблиці та спокійніше оформлення

- Перенесено виправлення виключених кодів МКХ та офлайн-оболонки з v20.
- Додано нормалізацію пунктів, префіксів, пунктуації та пробілів у діапазонах.
- Переставлені сусідні літери допускаються лише як явно позначений неточний збіг;
  точні результати показуються раніше.
- Запит «47б» веде одразу до пункту «б», без автоматичного додавання в кошик.
- Усі 19 таблиць доступні на весь екран і з повного роз’яснення; шрифт 14 px,
  легке чергування рядків, заголовки та числа читабельніші в обох темах.
- Захист від застосування старої сітки до зміненого табличного тексту.
- Світлі поверхні прохолодніші; фоновий рух слабший, пульсацію вибраного пункту прибрано.
- 97 нормативних файлів збережені побайтово. Подробиці: `QA-V21.md`.

---

# v19 — завершення рекомендацій після v18

- Експорт приховано в додатковому розділі кошика, логіку збережено.
- Головна компактніша, без дубльованих кроків і довгих інструкцій.
- Завершення локальної сесії з очищенням старих форматів та сигналом іншим вкладкам.
- Повний офлайн-маніфест для Next і Vinext, цілісне встановлення та кероване оновлення.
- Зафіксована технічна звірка 2570 фрагментів із джерелом 09.09.2026.
- Додано сценарії приймання та CSV всіх 87 статей для двох експертів.
- 88 тестів PASS; повні результати та межі — `QA-V19.md`.

---

# v14 — командно-медичне оформлення

Контрольний пакет для тестового деплою, 08.09.2026.

## Змінено

- Поєднано темно-смарагдову командну оболонку зі світлими робочими панелями
  та латунними акцентами. Додано емблему, фонову текстуру й водяні знаки.
- Оформлено шапку, навігацію, вибраний пункт, підготовку громадянина та
  паспорт норми. Ролі «Лікар» і «Громадянин» та логіку роботи збережено.
- Додано повільну фонову анімацію та підсвічування пункту з явним вимкненням
  для `prefers-reduced-motion`. Усунуто CSS-конфлікт позиціювання шапки.
- Оновлено іконки застосунку та список ресурсів service worker; версію кешу
  підвищено до v4. Уточнено умови запуску моніторингу й інструкції деплою.
- Додано чотири регресійні тести структури оформлення. Вони не перевіряють
  реальний вигляд у браузері.

## Фактичні перевірки фінального коду

Середовище: Node.js 24.19.0, npm 11.9.0, наявні встановлені залежності.

| Перевірка | Статус | Результат |
| --- | --- | --- |
| `npm run lint` | PASS | Без помилок |
| `npm run build:next` | PASS | Next.js 16.2.6, TypeScript, статичні сторінки |
| `npm run build` | PASS | vinext/Cloudflare build завершено |
| `node --test tests/*.test.mjs` | PASS | 63 тести, 0 помилок, 0 пропусків |
| Порівняння з v13 | PASS | Усі 109 файлів `lib/`, `package.json`, lockfile і `vercel.json` незмінні побайтово |
| Чисте перевстановлення `npm ci` | BLOCKED | Спробу в ізольованій папці зупинено на запиті дозволу середовища; повторного встановлення не виконано |
| Браузерний та візуальний аудит | BLOCKED | Браузер недоступний; див. `design-qa.md` |
| Деплой у Vercel-акаунті користувача | Не виконувався | Надано вихідний ZIP та інструкцію |
| Живе джерело, запуск моніторингу та іменні експертні перевірки | Не виконувалися в v14 | Чинність редакції на сьогодні не підтверджено |

Збірка vinext попереджає про клієнтський chunk понад 500 kB та неповну
статичну класифікацію маршруту. Це не помилки збірки, але фактичну швидкість
завантаження потрібно оцінити на Preview, особливо з мобільного інтернету.

## Межа відповідальності цього випуску

Нормативний корпус залишено з редакцією **22.08.2025**. Макети й нове
оформлення не є юридичним джерелом. Автоматичні інваріанти та тести не
становлять експертного підтвердження й не гарантують нормативної точності.
Окремий модуль перевірки процедури не додано. Авторизацію, аналітику й
передавання медичних даних не додано.

Наступна дія: тестовий Vercel Preview за `DEPLOY-VERCEL.md`, потім
браузерна перевірка та незалежна перевірка нормативного змісту до реального
застосування лікарями або громадянами.
